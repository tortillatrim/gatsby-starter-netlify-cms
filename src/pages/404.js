import React from 'react'
import Layout from '../components/Layout'

const NotFoundPage = () => (
  <Layout>
    <div className="section container content">
      <h1>Ikke funnet</h1>
      <p>Siden du ber om finnes ikke.</p>
    </div>
  </Layout>
)

export default NotFoundPage
